"use client";

// This component is deprecated - no longer used in the builder.
// Keeping as a stub to prevent compile errors from orphaned imports.
export function SummarySection() {
  return null;
}
